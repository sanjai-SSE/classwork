#include <stdio.h>


int main()
{
    int arr[SIZE] = {2, 4, 6, 8, 10};
    int product = 1;

    for (int i = 0; i < SIZE; i++) {
        product *= arr[i];
    }

    printf("Product of array elements: %d\n", product);

    return 0;
}
